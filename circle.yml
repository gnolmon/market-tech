machine:
  ruby:
    version:
      2.2.3

  environment:
    CIRCLE_ENV: production
dependencies:
  post:
    - bundle exec rake assets:precompile
production:
  post:
    - bundle exec rake production:custom
